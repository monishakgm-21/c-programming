#include <stdio.h>

int main()
{
  char c;
  int v=0;
  while((c=getchar())!='\n')
  {
      if(c=='a' || c=='e' || c=='i' || c=='o' || c=='u')
      {
          v++;
      }
  }
  printf("%d",v);
  

    return 0;
}
