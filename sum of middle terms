#include <stdio.h>
#include <math.h>
int main()
{
  int n,s=0,r,o,d=0,a,b;
  scanf("%d",&n);
  o=n;
  while(n!=0)
  {
      r=n%10;
      s+=r;
      d++;
      n=n/10;
  }
  a=o%10;
  b=o/pow(10,(d-1));
  printf("%d",s-(a+b));

    return 0;
}
