#include <stdio.h>
#include <math.h>
int main()
{
      int n,d=0,o,r,a,b;
      scanf("%d",&n);
      o=n;
      while(n!=0)
      {
          r=n%10;
          d++;
          n=n/10;
      }
      a=o%10;
      b=o/pow(10,(d-1));
      printf("%d",a+b);
    return 0;
}
