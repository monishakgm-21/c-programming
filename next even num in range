#include <stdio.h>

int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a%2==0 && b%2==0)
    {
        printf("%d",a+2);
    }
    else
    {
        printf("%d and %d are not even",a,b);
    }

    return 0;
}
